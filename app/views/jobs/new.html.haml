= title "Nova zaposlitev"
= error_messages_for :job

- form_for(@job) do |form|
  .fieldset
    = partial 'jobs/form', :form => form
  .buttons
    = form.submit 'Ustvari', :disable_with => 'Prosimo počakajte...'
