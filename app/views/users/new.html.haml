#people
  .row
    %h2
      Registracija
    -form_for @user do |form|
      = render :partial => '/users/form', :object => form
      = form.submit 'Registracija', :disable_with => 'Prosimo počakajte...'
