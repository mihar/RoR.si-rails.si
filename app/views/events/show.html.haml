- content_for :javascripts do
  = event_map @event

= title @event.title
.details
  %p.location
    = @event.location
  %p.date
    = @event.date.to_s(:fancy_date_with_time)

  .description
    = @event.cached_description_html

.crud
  %p= link_to 'Uredi', edit_event_path(@event)

/
  #map
